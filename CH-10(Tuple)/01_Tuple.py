""" 
Tuple is a class 
Tuple is iterable 
Tuple is immutable 
Tuple is hashable 
Tuple is a sequence
Tuple is hetrogeneous  just like list 

We can remove parenthesis in tuple Unlikely List 
In tuple we can not take direct input we have do it through list compren
"""


""" 
Yes, that's correct. In Python, a tuple is an immutable sequence of elements, which means that once a tuple is created, its elements cannot be changed. Therefore, you cannot directly take input for a tuple like you can for a list because you cannot modify a tuple after it is created.

When you take input for a tuple, you need to create a new tuple with the desired elements. This can be done using a variety of techniques, such as taking input for each element separately, using a loop to take input for multiple elements, or using a list comprehension to generate a list of elements that can be converted to a tuple.

In summary, the reason you cannot take direct input for a tuple is that tuples are immutable and their elements cannot be changed once they are created. Therefore, you need to create a new tuple with the desired elements.

"""